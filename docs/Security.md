# Security

- Authentication: Laravel Fortify
- Authorization: Gate policies for CRM models + Role middleware
- CSRF: Enabled by default
- Input Validation: Controllers validate uploads and forms
- Data Protection: Attachments and tickets follow polymorphic ownership via morph map
- Logging: Conversion service and data transfer controller log key events
